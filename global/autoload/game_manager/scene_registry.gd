# scene_registry.gd
# Deprecated — scene routing now lives in GameManager._SCENES (const Dictionary).
# This file is kept as an empty stub to avoid dangling references during migration.
# Delete once all call sites have moved to GameManager.go_to().
class_name SceneRegistry
extends Resource
