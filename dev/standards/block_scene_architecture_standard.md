# Block Scene Architecture Standard

This file has been superseded by narrower standards:

- `dev/standards/gdscript_structure_standard.md` for GDScript declaration order, section order, signal wiring, packed-scene instantiation order, and component `setup()` / `_apply()` shape
- `dev/standards/scene_node_source_standard.md` for persistent scene nodes, runtime node creation exceptions, and `node-src` markers
- `dev/standards/component_scene_standard.md` for reusable component layout, previewability, and placeholder defaults

Do not add new rules here. Update the narrower standard that owns the rule.
